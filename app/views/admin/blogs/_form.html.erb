<%= form_with(model: admin_blog, local: true) do |form| %>
  <% if admin_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_blog.errors.count, "error") %> prohibited this admin_blog from being saved:</h2>

      <ul>
      <% admin_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
